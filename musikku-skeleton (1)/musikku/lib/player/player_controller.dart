// Wrapper buat just_audio (atau player lain)

class PlayerController {
  // TODO: Future<void> play(Song song)
  //   -> kalau song.localCachePath != null, play dari situ (offline)
  //   -> kalau null, minta presigned URL dulu ke R2Service, baru streaming

  // TODO: pause(), resume(), seek(), next(), previous()
}
